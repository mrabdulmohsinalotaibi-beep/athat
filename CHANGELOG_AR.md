# سجل إصلاحات منصة الذات

تمت مراجعة النسخة المرفقة وإصلاح أخطاء مؤكدة في التشغيل والبناء.

## الإصلاحات المنفذة

- تحديث استدعاء دالة DeepSeek إلى واجهة TanStack Start الحالية باستخدام `middleware` و`inputValidator`.
- تصحيح استدعاء دالة DeepSeek من الواجهة إلى صيغة `{ data: ... }`.
- تصحيح قراءة `DEEPSEEK_API_KEY` المتوافقة مع TypeScript الصارم.
- إصلاح خاصية `rows5` غير الصحيحة في ملصق التوجيه الأسبوعي إلى `rows`.
- إصلاح روابط وإعادات التوجيه إلى صفحة المصادقة بإضافة معامل البحث `next` المطلوب.
- معالجة تحذيرات TypeScript عند قراءة صفوف ملفات CSV.
- إضافة أدوات موحدة للتاريخ الهجري (أم القرى) في `src/lib/hijri.ts`.
- تحويل التاريخ الظاهر في الترويسات والسجلات إلى الهجري.
- معالجة انزياح تاريخ اليوم الناتج عن المنطقة الزمنية في لوحة التحكم والبرامج.

## الذكاء الاصطناعي

المشروع يحتوي على تكاملين:

1. `DEEPSEEK_API_KEY`: إنشاء محتوى الملصق التوجيهي الأسبوعي.
2. `LOVABLE_API_KEY`: صياغة مسودات الحالات والتقارير والإحالات، ومطابقة أعمدة Excel أثناء الاستيراد.

يجب إعداد المفاتيح في بيئة الخادم/منصة الاستضافة، وعدم وضعها داخل ملفات الواجهة أو رفعها إلى المستودع.

## التحقق

- TypeScript: ناجح.
- Vite build: ناجح.

> ملاحظة: حقول الإدخال التي تحفظ تواريخ ISO في قاعدة البيانات بقيت بصيغة HTML القياسية داخليًا لضمان التوافق مع Supabase، بينما العرض الرسمي للمستخدم والتقارير والسجلات أصبح هجريًا. تحويل محرر الإدخال نفسه إلى إدخال هجري كامل يحتاج طبقة تحويل إضافية قبل الحفظ.
